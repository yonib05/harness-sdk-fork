name: "TypeScript: Release"

# Manual release of `@strands-agents/sdk` to npm.
#
# Shape: scan → ts-check/ts-test/integ/audit/pack → inspect → notes →
#        approve → tag + release → publish.
#
# Publish is the last step. Everything before it runs on a fork too — only
# the OIDC upload fails there, because the fork is not the registered
# trusted publisher.

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Explicit version, e.g. 1.4.0 (no leading v, no prefix).'
        required: true
        type: string
      sha:
        description: 'Optional commit SHA to release (must be an ancestor of origin/main). Defaults to current origin/main.'
        required: false
        default: ''
        type: string
      dry_run:
        description: 'Skip approval + tag + publish. Build/inspect/notes still run so you can review the artifact on the run page.'
        required: true
        default: true
        type: boolean
      run_integ_tests:
        description: 'Run integration tests. No effect on upstream (integ always runs there). On a fork: false skips integ; true runs integ but requires the fork to have its own AWS credentials configured.'
        required: true
        default: false
        type: boolean

concurrency:
  group: release-typescript
  cancel-in-progress: false

jobs:
  scan-commits:
    name: Resolve SHA and validate version
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      release_sha: ${{ steps.scan.outputs.release_sha }}
      prev_tag: ${{ steps.scan.outputs.prev_tag }}
      new_tag: ${{ steps.scan.outputs.new_tag }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          fetch-tags: true
          persist-credentials: false

      - name: Resolve SHA, find previous tag, validate version
        id: scan
        env:
          NEW_VERSION: ${{ inputs.version }}
          SHA_INPUT: ${{ inputs.sha }}
        run: |
          set -euo pipefail

          # 1. Validate the typed version (bare semver, no prefix).
          if ! [[ "$NEW_VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::version '$NEW_VERSION' is not bare semver (expected MAJOR.MINOR.PATCH, no 'v')."
            exit 1
          fi

          # 2. Resolve the SHA. Default to origin/main; if a sha was typed,
          #    accept it only if it's an ancestor of origin/main.
          if [ -n "$SHA_INPUT" ]; then
            RELEASE_SHA=$(git rev-parse --verify "$SHA_INPUT^{commit}" 2>/dev/null) || {
              echo "::error::sha '$SHA_INPUT' is not a valid commit."
              exit 1
            }
            if ! git merge-base --is-ancestor "$RELEASE_SHA" origin/main; then
              echo "::error::sha $RELEASE_SHA is not an ancestor of origin/main — release only from main history."
              exit 1
            fi
          else
            RELEASE_SHA=$(git rev-parse origin/main)
          fi

          # 3. Pick tag prefix and resolve previous tag.
          TAG_PREFIX="typescript/v"
          PREV_TAG=$(git tag --list "${TAG_PREFIX}*" --sort=-v:refname | head -n1)
          if [ -z "$PREV_TAG" ]; then
            echo "::error::No prior tag matching ${TAG_PREFIX}* — refusing to release without a baseline. (Fork-testing? Run \`git fetch upstream --tags && git push origin --tags\` first.)"
            exit 1
          fi
          PREV_VERSION="${PREV_TAG#"${TAG_PREFIX}"}"
          NEW_TAG="${TAG_PREFIX}${NEW_VERSION}"

          # 4. Reject duplicate / non-monotonic / pre-existing tags.
          if [ "$NEW_VERSION" = "$PREV_VERSION" ]; then
            echo "::error::version $NEW_VERSION matches existing tag $PREV_TAG."
            exit 1
          fi
          HIGHER=$(printf '%s\n%s\n' "$PREV_VERSION" "$NEW_VERSION" | sort -V | tail -n1)
          if [ "$HIGHER" != "$NEW_VERSION" ]; then
            echo "::error::version $NEW_VERSION is not greater than previous $PREV_VERSION."
            exit 1
          fi
          # Require a single-step increment (major, minor, or patch) so a typo
          # like 1.48.0 -> 1.84.0 fails here instead of relying on the approver.
          # Validate the baseline first: a malformed previous tag (prerelease
          # suffix, non-numeric part) must fail with a clear message, not a
          # cryptic arithmetic error under set -e.
          if ! [[ "$PREV_VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::previous tag version '$PREV_VERSION' is not bare semver -- cannot compute the increment baseline."
            exit 1
          fi
          IFS=. read -r P_MAJ P_MIN P_PAT <<< "$PREV_VERSION"
          VALID="$((P_MAJ+1)).0.0 ${P_MAJ}.$((P_MIN+1)).0 ${P_MAJ}.${P_MIN}.$((P_PAT+1))"
          case " $VALID " in
            *" $NEW_VERSION "*) ;;
            *)
              echo "::error::version $NEW_VERSION is not a single increment of $PREV_VERSION (expected one of: $VALID)."
              exit 1;;
          esac
          if git rev-parse --verify "refs/tags/$NEW_TAG" >/dev/null 2>&1; then
            echo "::error::tag $NEW_TAG already exists."
            exit 1
          fi

          # 5. Sanity-check: at least one commit since prev tag.
          COUNT=$(git rev-list --count "$PREV_TAG..$RELEASE_SHA")
          if [ "$COUNT" = "0" ]; then
            echo "::error::no commits between $PREV_TAG and $RELEASE_SHA — nothing to release."
            exit 1
          fi

          {
            echo "release_sha=$RELEASE_SHA"
            echo "prev_tag=$PREV_TAG"
            echo "new_tag=$NEW_TAG"
          } >> "$GITHUB_OUTPUT"

          {
            echo "### Release scan"
            echo ""
            echo "| | |"
            echo "|---|---|"
            echo "| Previous tag | \`$PREV_TAG\` (v$PREV_VERSION) |"
            echo "| Proposed tag | \`$NEW_TAG\` (v$NEW_VERSION) |"
            echo "| Pinned SHA | \`$RELEASE_SHA\` |"
            echo "| Commits since prev tag | **$COUNT** |"
          } >> "$GITHUB_STEP_SUMMARY"

  # ── Code quality + unit-test gates ─────────────────────────────────────
  ts-check:
    name: TypeScript code quality
    needs: scan-commits
    uses: ./.github/workflows/typescript-ts-check.yml
    permissions:
      contents: read
    with:
      ref: ${{ needs.scan-commits.outputs.release_sha }}

  ts-test:
    name: TypeScript unit tests
    needs: scan-commits
    uses: ./.github/workflows/typescript-ts-test.yml
    permissions:
      contents: read
    with:
      ref: ${{ needs.scan-commits.outputs.release_sha }}

  # ── Integration tests ──────────────────────────────────────────────────
  # Always runs on upstream. On a fork: opt-in via `run_integ_tests: true`
  # (fork needs its own AWS credentials).
  integ:
    name: TypeScript integration tests
    needs: scan-commits
    if: github.event.repository.fork != true || inputs.run_integ_tests == true
    uses: ./.github/workflows/typescript-integration-test.yml
    permissions:
      id-token: write
      contents: read
      pull-requests: read
    secrets: inherit
    with:
      ref: ${{ needs.scan-commits.outputs.release_sha }}
      full_suite: true

  # ── Security audit (informational) ─────────────────────────────────────
  security-audit:
    name: TypeScript security audit
    needs: scan-commits
    uses: ./.github/workflows/typescript-security-audit.yml
    permissions:
      contents: read
    with:
      ref: ${{ needs.scan-commits.outputs.release_sha }}

  # ── Pack + smoke test (uploads npm-build-output artifact) ──────────────
  package-pack:
    name: TypeScript package-pack smoke test
    needs: scan-commits
    uses: ./.github/workflows/typescript-test-package-pack.yml
    permissions:
      contents: read
    with:
      ref: ${{ needs.scan-commits.outputs.release_sha }}
      version: ${{ inputs.version }}

  # ── Inspect the packed tarball ─────────────────────────────────────────
  # Downloads npm-build-output, lists tarball contents + package.json,
  # checks the version, re-uploads as npm-pack-bundle. publish-npm pulls
  # from the same bundle.
  inspect:
    name: Inspect npm tarball
    needs: package-pack
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Download build output
        uses: actions/download-artifact@v8.0.1
        with:
          name: npm-build-output
          path: dist-npm

      - name: List tarball contents + package.json
        run: |
          set -euo pipefail
          for tgz in dist-npm/*.tgz; do
            echo "::group::$tgz contents"
            tar tzf "$tgz"
            echo "::endgroup::"
            echo "::group::$tgz package.json"
            tar -xOzf "$tgz" package/package.json
            echo "::endgroup::"
          done

      - name: Assert tarball is stamped with the typed version
        env:
          EXPECTED: ${{ inputs.version }}
        run: |
          set -euo pipefail
          checked=0
          errors=0
          for tgz in dist-npm/*.tgz; do
            actual=$(tar -xOzf "$tgz" package/package.json | jq -r .version)
            checked=$((checked + 1))
            if [ "$actual" != "$EXPECTED" ]; then
              echo "::error::$tgz: version is '$actual', expected '$EXPECTED'."
              errors=$((errors + 1))
            fi
          done
          if [ "$checked" -eq 0 ]; then
            echo "::error::No .tgz found under dist-npm/"
            exit 1
          fi
          if [ "$errors" -gt 0 ]; then
            exit 1
          fi
          echo "All $checked tarball(s) stamped with version $EXPECTED"

      - name: Upload verified tarball for publish
        uses: actions/upload-artifact@v7.0.1
        with:
          name: npm-pack-bundle
          path: dist-npm/*.tgz
          if-no-files-found: error
          retention-days: 30

  # ── Draft notes grouped by commit type ─────────────────────────────────
  draft-notes:
    name: Draft release notes (grouped by commit type)
    needs:
      - scan-commits
      - ts-check
      - ts-test
      - integ
      - security-audit
      - package-pack
      - inspect
    if: |
      always() &&
      needs.scan-commits.result == 'success' &&
      needs.ts-check.result == 'success' &&
      needs.ts-test.result == 'success' &&
      (needs.integ.result == 'success' || needs.integ.result == 'skipped') &&
      needs.security-audit.result == 'success' &&
      needs.package-pack.result == 'success' &&
      needs.inspect.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ needs.scan-commits.outputs.release_sha }}
          fetch-depth: 0
          fetch-tags: true
          persist-credentials: false

      - name: Render notes
        env:
          PREV_TAG: ${{ needs.scan-commits.outputs.prev_tag }}
          NEW_REF: ${{ needs.scan-commits.outputs.release_sha }}
          NEW_TAG: ${{ needs.scan-commits.outputs.new_tag }}
        run: |
          set -euo pipefail
          # Feed commits to the grouper as `hash<TAB>subject` lines and let
          # it bucket them by conventional-commit type (feat, fix, ...).
          git log --no-merges --pretty=format:'%h%x09%s' "$PREV_TAG..$NEW_REF" \
            | NEW_TAG="$NEW_TAG" PREV_TAG="$PREV_TAG" \
              node .github/scripts/group-release-notes.mjs > release-notes.md

      - name: Render release summary
        env:
          NEW_TAG: ${{ needs.scan-commits.outputs.new_tag }}
          RELEASE_SHA: ${{ needs.scan-commits.outputs.release_sha }}
          DRY_RUN: ${{ inputs.dry_run }}
          IS_FORK: ${{ github.event.repository.fork }}
        run: |
          set -euo pipefail
          {
            echo "## Release proposal"
            echo ""
            echo "| | |"
            echo "|---|---|"
            echo "| Package | \`@strands-agents/sdk\` (TypeScript) |"
            echo "| Proposed tag | \`$NEW_TAG\` |"
            echo "| Pinned SHA | \`$RELEASE_SHA\` |"
            echo "| Dry run | \`$DRY_RUN\` |"
            echo "| Running on fork | \`$IS_FORK\` |"
            echo ""
            echo "> Reviewers: the verified tarball is uploaded as \`npm-pack-bundle\` on this run's page. Download and install in a fresh tmpdir to sanity-check before approving."
            echo ""
            echo "### Drafted notes"
            echo ""
            cat release-notes.md
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload release notes artifact
        uses: actions/upload-artifact@v7.0.1
        with:
          name: release-notes
          path: release-notes.md
          retention-days: 30

  # ── Reviewer approval (skipped on dry runs) ────────────────────────────
  approve-release:
    name: Wait for reviewer approvals
    needs: [scan-commits, draft-notes]
    if: inputs.dry_run != true
    runs-on: ubuntu-latest
    environment:
      # Shared with release-python.yml. Configure reviewers + main-only
      # deployment branches in repo settings.
      name: release-gate
    permissions: {}
    steps:
      - name: Acknowledge approval
        env:
          NEW_TAG: ${{ needs.scan-commits.outputs.new_tag }}
          RELEASE_SHA: ${{ needs.scan-commits.outputs.release_sha }}
        run: |
          echo "Approved to release $NEW_TAG at $RELEASE_SHA."

  # ── Tag + GitHub release (before publish, runs on forks too) ───────────
  # Runs before publish so the tag is the source of truth. If publish
  # fails, the tag stays — just re-run publish. Runs on forks too; creates
  # the release on the fork's repo so fork-tests go end-to-end.
  create-gh-release:
    name: Create GitHub release
    needs: [scan-commits, draft-notes, approve-release]
    if: inputs.dry_run != true
    runs-on: ubuntu-latest
    permissions:
      contents: write
      # discussions: write lets the announcement step below create the
      # release-linked discussion.
      discussions: write
      # actions: write lets this job dispatch the changelog-sync workflow below.
      actions: write
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ needs.scan-commits.outputs.release_sha }}
          # This job creates the tag, so keep the GITHUB_TOKEN in git config
          # for the push below. Other jobs use persist-credentials: false.
          persist-credentials: true

      - name: Download release notes artifact
        uses: actions/download-artifact@v8.0.1
        with:
          name: release-notes

      - name: Tag the pinned SHA and create the release
        env:
          GH_TOKEN: ${{ github.token }}
          NEW_TAG: ${{ needs.scan-commits.outputs.new_tag }}
          RELEASE_SHA: ${{ needs.scan-commits.outputs.release_sha }}
        run: |
          set -euo pipefail
          # Push the tag over git rather than letting `gh release create
          # --target <sha>` create it through the releases API. The API path
          # returns "403 Resource not accessible by integration" for the
          # GITHUB_TOKEN (see cli/cli#9514); a plain ref push with the same
          # token succeeds.
          #
          # Both steps are guarded so re-running the job after a partial
          # failure is safe: if a prior run pushed the tag but errored before
          # the release was made, git push would otherwise refuse the existing
          # tag. This keeps the "tag is source of truth, just re-run" contract.
          if ! git ls-remote --exit-code --tags origin "refs/tags/$NEW_TAG" >/dev/null 2>&1; then
            git tag "$NEW_TAG" "$RELEASE_SHA"
            git push origin "refs/tags/$NEW_TAG"
          fi

          # Tag now exists on the remote, so this only attaches notes to it.
          if ! gh release view "$NEW_TAG" >/dev/null 2>&1; then
            gh release create "$NEW_TAG" \
              --title "$NEW_TAG" \
              --notes-file release-notes.md
          fi

      # Post the release notes to the Discussions board. Runs after the
      # release exists and is non-fatal (same contract as the changelog
      # dispatch below): a missing or renamed category must not fail a
      # release whose tag is already pushed. A category PATCH on a published
      # release creates and links the discussion; re-runs are a no-op once
      # one is linked.
      - name: Post announcement discussion
        env:
          GH_TOKEN: ${{ github.token }}
          NEW_TAG: ${{ needs.scan-commits.outputs.new_tag }}
          # Empty on forks: a fork may not have Discussions or this
          # category configured.
          DISCUSSION_CATEGORY: ${{ github.event.repository.fork != true && 'Announcements' || '' }}
        run: |
          set -euo pipefail
          if [ -z "$DISCUSSION_CATEGORY" ]; then
            echo "Fork run; skipping the announcement discussion."
            exit 0
          fi
          RELEASE_ID=$(gh api "repos/$GITHUB_REPOSITORY/releases/tags/${NEW_TAG//\//%2F}" --jq .id) \
            && gh api -X PATCH "repos/$GITHUB_REPOSITORY/releases/$RELEASE_ID" \
                 -f discussion_category_name="$DISCUSSION_CATEGORY" --jq .discussion_url \
            || echo "::warning::Failed to post the $NEW_TAG announcement discussion; create it from the release page."

      # Trigger the changelog sync directly. A release created with the built-in
      # GITHUB_TOKEN (above) does NOT emit a `release` event, by design, so the
      # Changelog: Sync workflow's `on: release` trigger never fires from here --
      # dispatch it explicitly instead. Non-fatal by design (`|| echo`): a
      # changelog hiccup must not fail a release that already published its tag,
      # and the daily cron backstops -- but surface a warning annotation so the
      # failure is visible now rather than only after the ~24h cron.
      - name: Trigger changelog sync
        env:
          GH_TOKEN: ${{ github.token }}
          NEW_TAG: ${{ needs.scan-commits.outputs.new_tag }}
        run: |
          gh workflow run changelog-sync.yml -f tag="$NEW_TAG" \
            || echo "::warning::Failed to dispatch changelog sync for $NEW_TAG; the daily cron will backstop."

  # ── Publish to npm ─────────────────────────────────────────────────────
  # Last step. On a fork this fails at the OIDC step — by design.
  publish-npm:
    name: Publish to npm
    needs: [inspect, approve-release, create-gh-release]
    if: inputs.dry_run != true
    runs-on: ubuntu-latest
    environment:
      name: npm
      url: https://www.npmjs.com/package/@strands-agents/sdk
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Update npm to latest
        # Trusted publishing needs npm >= 11.5.1.
        run: npm install -g npm@latest

      - name: Download verified tarball
        uses: actions/download-artifact@v8.0.1
        with:
          name: npm-pack-bundle
          path: dist-npm

      - name: Publish to npm
        run: |
          set -euo pipefail
          shopt -s nullglob
          tgzs=(dist-npm/*.tgz)
          if [ "${#tgzs[@]}" -ne 1 ]; then
            echo "::error::Expected exactly 1 tarball under dist-npm/, found ${#tgzs[@]}."
            ls -la dist-npm/ || true
            exit 1
          fi
          # ./ prefix: npm treats a bare "dir/file.tgz" as a git spec.
          npm publish "./${tgzs[0]}" --access public --tag latest

  # ── Post-publish smoke check ───────────────────────────────────────────
  # Poll npm until the new version is visible, replacing the manual
  # "check the package page" verification. Fails loudly if the registry
  # doesn't show the version within the timeout.
  verify-published:
    name: Verify version on npm
    needs: [publish-npm]
    if: inputs.dry_run != true
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: Poll npm for the new version
        env:
          VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          for i in $(seq 1 30); do
            # Cache-buster: registry CDNs cache negative (404) responses; a poll
            # that lands before propagation must not pin a stale 404 for the
            # rest of the window.
            if curl -sf "https://registry.npmjs.org/@strands-agents/sdk/$VERSION?t=$(date +%s)" >/dev/null; then
              echo "@strands-agents/sdk $VERSION is live on npm."
              exit 0
            fi
            echo "attempt $i/30: not visible yet, retrying in 10s..."
            sleep 10
          done
          echo "::error::@strands-agents/sdk $VERSION not visible on npm after 5 minutes."
          exit 1
